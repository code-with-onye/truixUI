import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';

import { Button, Props } from '../src/Button';

<Meta title="Components/Button" component={Button} />

export const Template = (args) =><Button {...args} />

# Button

Let's define a story for our `Button` component:

<Story name="Button" args={{ children: 'Default'}}>
    {Template.bind({})}
</Story>

<ArgsTable story="Button"  />

Extra  Description
<Canvas>
   <Story name="primary" args={{ children: 'Prmary', variant:'default'}}>
        {Template.bind({})}
    </Story>
    <Story name="dark" args={{ children: 'Dark', variant:'dark'}}>
        {Template.bind({})}
    </Story>
    <Story name="light" args={{ children: 'Light', variant:'light'}}>
        {Template.bind({})}
    </Story>
</Canvas>

